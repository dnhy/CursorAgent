---
alwaysApply: true
description: 代码风格规范
---

# 代码风格规范

- 所有新代码使用 TypeScript,保持强类型与显式返回值
- 遵循《代码整洁之道》
- 遵循项目的lint、prettier规则
- 优先使用 async/await 而不是回调,并发场景仍应配合 Promise.all，事件流场景可用回调/监听
- 编写完善的错误处理，记录日志 + 业务兜底 + 用户可见信息走 i18n
- 优先函数式风格
- 遵循开放封闭，组合大于继承的原则
- 组件和函数遵循单一职责原则
- 从一个对象解构取值到另一个对象时，禁止使用类似 `...col` 的整体展开，必须显式列出并解构所有需要的字段，在少数框架/工具级场景可酌情使用
